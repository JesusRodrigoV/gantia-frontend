export interface SensitivitySettings {
  swipe_threshold: number;
  swipe_dominance: number;
  swipe_cooldown: number;
  posture_hold_time: number;
  mouse_speed: number;
  mouse_dead_zone: number;
  double_tap_window: number;
  tilt_threshold: number;
  tilt_cooldown: number;
}
